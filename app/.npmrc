# jest-expo@57 peers on @react-native/jest-preset@^0.85 while RN 0.86 ships 0.86,
# a transient upstream peer-range lag right after the SDK release. legacy-peer-deps
# lets `npm install` (and CI) resolve cleanly; remove once jest-expo widens the peer.
legacy-peer-deps=true
